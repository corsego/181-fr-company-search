<%= turbo_stream.update :results do %>
  <% @results["results"].each do |result| %>
    <div data-action="click->company-autocomplete#paste" style="border: 1px solid black; margin: 10px; padding: 10px;">
      <%= result["nom_complet"] %>
      <%= result["siren"] %>
    </div>
  <% end %>
<% end %>