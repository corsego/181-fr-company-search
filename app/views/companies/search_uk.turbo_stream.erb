<%= turbo_stream.update :results do %>
  <% @results["items"].each do |result| %>
    <div data-action="click->company-autocomplete#paste" data-companyid="<%= result["company_number"] %>" style="border: 1px solid black; margin: 10px; padding: 10px;">
      <%= result["title"] %>
      <%= result["company_number"] %>
      <%= result["address_snippet"] %>
    </div>
  <% end %>
<% end %>
